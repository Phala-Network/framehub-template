{
  "name": "phat-frame-template",
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npx @phala/fn build --experimentalAsync",
    "publish": "npx @phala/fn build --experimentalAsync && ts-node scripts/publish.ts"
  },
  "devDependencies": {
    "ts-node": ">=8.0.0",
    "typechain": "^8.1.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@coinbase/onchainkit": "^0.4.1",
    "@phala/fn": "^0.2.13",
    "@phala/sidevm-env": "^1.0.1",
    "viem": "^1.19.3"
  }
}
